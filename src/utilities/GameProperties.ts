export const enum GameProperties {
    gridSize = 17,
    gridTileSize = 32,
    levelWidth = gridSize * gridTileSize,
    levelHeight = gridSize * gridTileSize,
    levelMidX = levelWidth / 2,
    levelMidY = levelHeight / 2
}
